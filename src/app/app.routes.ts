import { Routes } from '@angular/router';
import { ProblemListComponent } from './core/problem-list/problem-list.component';

export const routes: Routes = [
    { path: '', component: ProblemListComponent },
];
